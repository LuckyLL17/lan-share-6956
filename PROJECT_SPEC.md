# 局域网共享服务项目规格

## 项目目标

提供一个无需云端账号的局域网文件共享服务，让同一网络内的设备能够发现彼此、浏览已授权目录、传输文件并交换简短消息。数据保存在运行设备本地，服务通过 HTTP API 和单页前端提供操作入口。

## 用户角色

- 本机用户：配置设备名称、创建或停用共享目录、查看传输和消息状态。
- 局域网访客设备：发现本机后读取已启用共享的目录清单和文件内容，或向可写共享上传文件。
- 后台发现循环：周期性广播设备信息并清理超过 TTL 的离线设备。

## 核心实体

- Device：设备标识、地址、端口、在线状态和最近心跳。
- Share：共享别名、根目录、写入权限和启用状态。
- Transfer：远端文件传输方向、断点、状态和错误信息。
- Message：局域网设备间的文本消息及已读状态。

## 业务流程

### 设备发现

局域网用户调用发现接口触发 UDP 广播；服务接收其他设备的公告并写入设备记录；设备列表接口返回当前仍在 TTL 内的设备。

### 共享目录浏览

本机用户创建共享并选择只读或可写权限；服务保存共享配置并仅暴露启用项；访客设备按别名浏览目录和文件，路径越界请求被拒绝。

### 文件传输

用户创建传输任务并指定远端共享、文件路径和本地目标；传输服务记录排队/运行/暂停/完成状态并支持从已有字节偏移继续；列表和进度接口返回可观察状态。

### 消息交换

用户发送广播或单播消息；UDP 层接收后写入消息记录；消息列表、未读计数和批量已读接口维护本地阅读状态。

## 状态与规则

- 设备在最近心跳超过 TTL 后转为 offline。
- 只有 enabled 的 Share 可被远端发现；readwrite 共享才允许上传。
- Transfer 状态只能按 queued、running、paused、done、failed、canceled 的业务转换推进。
- 文件路径必须限制在 Share 根目录内，拒绝 `..` 穿越。
- Message 的 read 标志只允许从未读变为已读，批量操作不删除正文。

## 接口与验证

- `GET /api/v1/health`：服务健康探针。
- `GET /api/v1/devices` 与 `POST /api/v1/devices/discover`：设备发现工作流。
- `GET/POST /api/v1/shares`、`PATCH /api/v1/shares/:id/toggle`：共享生命周期。
- `GET /api/v1/remote/shares` 与 `GET /api/v1/remote/shares/:alias/items`：远端浏览工作流。
- `GET/POST /api/v1/transfers` 与 `GET /api/v1/transfers/:id/progress`：传输状态工作流。
- `GET/POST /api/v1/messages` 与 `GET /api/v1/messages/unread`：消息交换工作流。

运行验证通过启动 HTTP 服务、请求健康探针和业务 API，并逐个检查设备、共享、远端资源、传输和消息接口的 JSON 响应。
